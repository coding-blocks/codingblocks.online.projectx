<div class="container c-doubt mt-5">
  {{#if recent}}
    Hi
  {{else}}
  <div class="d-flex head">
    <div class="heading-u mr-auto">
      Ask your doubts ({{topic_list.topics.length}})
    </div>
    <a href="{{redirectLink}}" target="_blank">
        <button class="button-solid right-head-button">Discussion Forum</button>
    </a>
  </div>

  <div class="row doubts-box m-0">
    <div class="col-12">
      {{#each topic_list.topics as |topic|}}
        {{doubt-view topicId=topic.id}}
      {{/each}}
    </div>
  </div>
  {{/if}}
</div>
