{{#each @runs as |run|}}
  <MyCoursesList::RunRow
    @run={{run}}
    as |progressPercent| >

    <MyCoursesList::ExpiredCourses::Content
      @progressPercent={{progressPercent}}
      @tagName=''
      @run={{run}} />

  </MyCoursesList::RunRow>
{{/each}}