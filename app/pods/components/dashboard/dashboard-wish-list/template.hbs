{{#each wishlist as |item|}}
  <div class="hover-grey px-5 py-4">
    <div class="row mx-0 align-items-center">
      <div class="col-2">
        <img src="{{item.course.logo}}" alt="">
      </div>
      <div class="col-10">
        <h5 class="bold">{{item.course.title}}</h5>
        <RatingStarsStatic @rating={{item.course.rating}} />
      </div>
    </div>
  </div>
{{/each}}
