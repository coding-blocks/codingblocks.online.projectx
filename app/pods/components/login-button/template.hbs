
{{#if session.isAuthenticated}}
  <button class="button-solid" onclick={{action 'invalidateSession'}}>LOGOUT</button>
{{else}} 
        <button class="button-solid" onclick={{action 'logIn'}}>
          {{#unless hasBlock}}
            LOGIN
          {{else}}
            {{yield}}
          {{/unless}}
        </button>
{{/if}}
