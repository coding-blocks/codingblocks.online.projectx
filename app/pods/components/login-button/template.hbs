
{{#if session.isAuthenticated}}
  <button onclick={{action 'invalidateSession'}}>Logout</button>
{{else}} 
<button>
  <a href="{{loginUrl}}" style="text-decoration: none; color: white">
    Login
  </a>
</button>
  
{{/if}}