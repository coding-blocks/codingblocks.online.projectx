<h1> My Courses </h1>

<WTabNav 
  @tabs={{tabs}}
  @activeTab={{activeTab}}
  @onTabChange={{action (mut activeTab)}}
  >
  <WAsync @task={{activeTab.task}} @autoFire={{not activeTab.task.performCount}} as |result|>
    {{component activeTab.component runs=result}}
  </WAsync>
</WTabNav>