/* eslint-env node */
'use strict';

module.exports = function(environment) {
  let ENV = {
    modulePrefix: 'hiring-blocks',
    podModulePrefix: 'hiring-blocks/pods',
    environment
  };

  return ENV;
};
