/* eslint-env node */
'use strict';

module.exports = function(environment) {
  let ENV = {
    modulePrefix: 'cricket-cup',
    podModulePrefix: 'cricket-cup/pods',
    environment
  };

  return ENV;
};
